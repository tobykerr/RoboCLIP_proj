#!/bin/bash -login
#SBATCH -o %x.o%j

#SBATCH -p serial             # use A100 80GB GPU
#SBATCH -t 10            # time limit of 1 day

conda activate roboclip

python tools/plot_reward_curve.py \
  --run-dir metaworld/drawer-open-v2-goal-hidden_max_max28 \
  --out-dir results/max28/new_plots \
  --n-envs 8 \
  --combined
